import { Text, View } from 'react-native';

// Placeholder — tela principal (lista de cerimônias)
export default function HomeScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Hauxe — em breve</Text>
    </View>
  );
}
