import { Text, View } from 'react-native';

// Placeholder — tela de login
export default function SignIn() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Sign In — em breve</Text>
    </View>
  );
}
